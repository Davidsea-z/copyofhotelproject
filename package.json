{
  "name": "hotel-report-webapp",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler pages dev . --ip 0.0.0.0 --port 3000",
    "clean-port": "fuser -k 3000/tcp 2>/dev/null || true"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
